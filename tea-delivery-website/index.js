import React from 'react';
import ReactDOM from 'react-dom/client';
import TeaDeliveryApp from './TeaDeliveryApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
        <TeaDeliveryApp />
    </React.StrictMode>
);
